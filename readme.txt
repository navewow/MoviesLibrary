Workers folder has the Reporting worker service that generates the below csv reports. The reporst are generated once in a day.
-- Actor Movies
-- Genre Movies
-- Genre Count

Movies Library uses sqlserver instance for DB please update teh connection string and run the application. 
Data seeding is configured to create DB and the required tables along with basic seed data.
Basic CRUD operation is scafolled so that it will be easy to manipulate the entire DB.
The APIs requested as per the requiremnt are created with design consideration.

Sln Stack
-- Net Core 8
-- EF Core 8
-- SQL Express